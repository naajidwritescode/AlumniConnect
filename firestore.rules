rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for development and reset workflows
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
